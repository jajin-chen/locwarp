# Upper bound is deliberate: 10.2 replaced the zeroconf-based bonjour
# helper with raw sockets that break mDNS on multi-NIC Windows. We browse
# via zeroconf ourselves now (services/tunnel_discovery.py), but keep the
# ceiling so the device-protocol layer can't shift under us unreviewed.
pymobiledevice3>=10.1.0,<10.3
pytun_pmd3>=3.0.3
developer_disk_image>=0.2.0
pyimg4>=0.8.8
fastapi>=0.139.0
uvicorn[standard]>=0.51.0
websockets>=16.0
gpxpy>=1.6.0
httpx>=0.27.0
pydantic>=2.13.0
python-multipart>=0.0.32
psutil>=6.0
zeroconf>=0.132
tzfpy>=1.3.2
tzdata>=2025.1
